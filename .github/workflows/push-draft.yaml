name: push draft to hatena blog

on:
  pull_request:
    paths:
      - "draft_entries/**"

jobs:
  push-draft:
    uses: hatena/hatenablog-workflows/.github/workflows/push-draft.yaml@56ae71eef63ed596ab63d7d99e86898e768a45c2 # v2.1.2
    secrets:
      OWNER_API_KEY: ${{ secrets.OWNER_API_KEY }}
