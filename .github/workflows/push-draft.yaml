name: push draft to hatena blog

on:
  pull_request:
    paths:
      - "draft_entries/**"

jobs:
  push-draft:
    uses: hatena/hatenablog-workflows/.github/workflows/push-draft.yaml@1fc8993fdb5727ca6172542e038e9c28d0d2bef0 # v2.0.13
    secrets:
      OWNER_API_KEY: ${{ secrets.OWNER_API_KEY }}
