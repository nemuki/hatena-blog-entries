name: push published entries


on:
  pull_request:
    paths:
      - "entries/**"

jobs:
  push-published-entries:
    uses: hatena/hatenablog-workflows/.github/workflows/push-published-entries.yaml@56ae71eef63ed596ab63d7d99e86898e768a45c2 # v2.1.2
    secrets:
      OWNER_API_KEY: ${{ secrets.OWNER_API_KEY }}
